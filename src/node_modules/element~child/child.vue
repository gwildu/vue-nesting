<style lang="less">

</style>

<template>

  <p>
    I'm a child and my value is <strong>{{value}}</strong>
    <button @click.prevent="update">update</button></p>


</template>

<script>

  var self = this;


  import {changeChildrenValue} from '../../vuex/actions.js';

//  var
//      actions = {
//        changeChildrenValue: require('../../vuex/actions').changeChildrenValue
//      };

  module.exports = {
    props: ['factor', 'data-hook'],
    data: function(){
      return {
      }
    },
    vuex: {
      getters: {
        parentChildren: function(state) {
          return state.parent
        }
      },
      actions: {
        changeChildrenValue //: this.actions.changeChildrenValue
      }
    },
    methods: {
      update: function (){
        this.changeChildrenValue(this.calc());
      },
      calc() {
        return Math.round(Math.random() * this.factor);
      }
    }
  };


</script>